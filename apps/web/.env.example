# Origin of the dashboard/auth app that "Get started" and "Sign in" link to
# (see src/lib/config.ts). Local dev defaults to this; set it to the deployed
# dashboard origin in production.
PUBLIC_APP_URL=http://localhost:3024
