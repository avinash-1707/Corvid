# Base URL of the Hono gateway (Better Auth is mounted at `${NEXT_PUBLIC_API_URL}/api/auth/*`).
# Public — it's embedded in the client bundle, since every gateway call happens in the browser.
NEXT_PUBLIC_API_URL=http://localhost:8787
